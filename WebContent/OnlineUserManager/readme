1.利用Servlet的功能制作一个在线用户管理模块，用户想要被管理，则必须要登录，未登录用户暂不进行处理
	考虑到登录的简化处理，只要用户输入的密码为“xia123”，就表示用户登录成功，此时登录就不考虑使用数据库了
	
2.所有的用户信息保存在一个application属性范围之中，同时该属性的内容保存有一个Map集合
	Map集合中，key是用户的id，而value就是当前用户的保存状态，已删除的用户保存一个true，为删除的用户保存一个false

3.如果管理员想要有效的进行控制，则应该考虑通过一个列表的形式进行控制，而这个列表就是依赖一个map集合迭代的方式实现

4.Login.jsp是不需要进行过滤的，/front下的资源，如果不进行登录或者被管理员强制下线了，是不允许被访问的。

