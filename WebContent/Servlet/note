Servlet是Java实现CGI的一种方式，它早于JSP
CGI：公共网关接口，它是一种技术标准，可以用各种各样的语言实现该标准
传统的CGI是基于多进程的方式来进行处理的，而Java是采用多线程的方式来实现该标准的
所以对比起传统的CGI，性能是非常高的

Servlet是一个独立的程序类，但是如果这个类想要实现HTTP请求的接收和处理，则在其结构定义上有明确要求的，
所有的Servlet类一定要继承HttpServlet父类